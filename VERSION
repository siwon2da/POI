1.12.0
POI 1.12.0 — 확장성: 동시성 + 네트워크. task — import 없이 task.run/wait/all/race/map,
task.channel(스레드 큐), task.every/after(타이머), task.lock. 문장 문법 background { } 와
every 1 second { }. http — get/post/put/delete/download, 응답 Box{status,ok,text,json,headers}.
net — 원시 TCP(tcp/listen/resolve). 안전 모드에서 http·net·task·background·every 차단.
poi.lock — poi add/install 이 정확한 버전 기록, poi install --frozen 으로 재현. 올리기:  poi update
