1.14.0
POI 1.14.0 — 프로덕션 & 개발자 경험. WSGI 어댑터(poi wsgi app.poi → gunicorn/waitress/uvicorn),
공용 디스패치(HTTP·WSGI 공유), /healthz 자동, POI_ENV=production 오류 마스킹, poi run --host/--prod.
poi doctor(환경 진단), poi new <이름> --web|--api|--cli(템플릿). examples/showcase — POI 로만 작성한
웹사이트(server·render·auth·미들웨어·DB·POI가 POI를 실행). 올리기:  poi update
