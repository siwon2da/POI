1.10.0
POI 1.10.0 — AI 모듈 + 구조 분해. `ai.chat(...)` / `ai.ask(...)` / `ai.code(...)` — 하온과
같은 백엔드(Groq → 로컬 Ollama), import 없이 바로. `{ name, age } = user` 와
`[a, b] = pair` 구조 분해. 안전 모드에서 ai·system·dotenv·compress 도 차단. 올리기:  poi update
